# ZeroController — Recommendation System dependencies
# Install: pip install -r requirements_recommendation.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.4.0
torch>=2.0.0        # already in requirements.txt
anthropic>=0.25.0   # optional — for LLM one-liner (Claude Haiku)
